<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity"
    android:background="#A0522D"
    >

<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_alignParentTop="true"
    android:layout_alignBottom="@+id/btnInicioComecar"
    android:layout_marginBottom="134dp"
    android:id="@+id/relativeLayout"></RelativeLayout>
    <Button
        android:layout_width="243dp"
        android:layout_height="80dp"
        android:id="@+id/btnInicioComecar"
        android:text="Começar"
        android:layout_weight="0.07"
        android:layout_marginTop="163dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:textColor="#333"
        />

    <Button
        android:layout_width="243dp"
        android:layout_height="80dp"
        android:text="Como Jogar"
        android:id="@+id/btnComoJogar"
        android:layout_below="@+id/btnInicioComecar"
        android:layout_alignLeft="@+id/btnInicioComecar"
        android:layout_alignStart="@+id/btnInicioComecar"
        android:textColor="#333"/>


</RelativeLayout>
